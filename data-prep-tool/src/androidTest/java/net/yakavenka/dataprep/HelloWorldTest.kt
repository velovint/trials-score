package net.yakavenka.dataprep

import androidx.test.ext.junit.runners.AndroidJUnit4
import org.junit.Assert.assertTrue
import org.junit.Test
import org.junit.runner.RunWith

/**
 * Minimal instrumented test to verify test infrastructure setup.
 * Part of Phase 1, Slice 1.2: Test Infrastructure Setup.
 */
@RunWith(AndroidJUnit4::class)
class HelloWorldTest {

    @Test
    fun testInfrastructure_passes() {
        // This test always passes - it proves the test infrastructure works
        assertTrue(true)
    }
}
